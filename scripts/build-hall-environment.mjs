import fs from 'node:fs/promises';import{gzipSync}from'node:zlib';
const{chromium}=await import(process.env.PLAYWRIGHT_MODULE||'playwright');const browser=await chromium.launch({headless:true,args:['--use-angle=d3d11']});
try{const page=await browser.newPage();await page.route('https://environment.local/**',async route=>{const name=new URL(route.request().url()).pathname;const file={'/three.js':'node_modules/three/build/three.module.js','/three.core.js':'node_modules/three/build/three.core.js','/room.js':'node_modules/three/examples/jsm/environments/RoomEnvironment.js'}[name];await route.fulfill({contentType:file?'text/javascript':'text/html',body:file?await fs.readFile(file,'utf8'):'<script type="importmap">{"imports":{"three":"/three.js"}}</script>'});});await page.goto('https://environment.local/');
const result=await page.evaluate(async()=>{const T=await import('/three.js'),{RoomEnvironment}=await import('/room.js');const r=new T.WebGLRenderer({antialias:true,alpha:false,stencil:false});r.outputColorSpace=T.SRGBColorSpace;r.toneMapping=T.ACESFilmicToneMapping;const generator=new T.PMREMGenerator(r),room=new RoomEnvironment(),target=generator.fromScene(room,.04);const pixels=new Uint16Array(target.width*target.height*4);await r.readRenderTargetPixelsAsync(target,0,0,target.width,target.height,pixels);const bytes=new Uint8Array(pixels.buffer);let s='';for(let i=0;i<bytes.length;i+=8192)s+=String.fromCharCode(...bytes.subarray(i,i+8192));const result={width:target.width,height:target.height,data:btoa(s),mapping:target.texture.mapping,min:Math.min(...pixels.subarray(0,1000)),max:pixels.reduce((m,v)=>Math.max(m,v),0)};target.dispose();generator.dispose();room.dispose();r.dispose();return result;});
const header=Buffer.alloc(8);header.writeUInt32LE(result.width,0);header.writeUInt32LE(result.height,4);const bytes=gzipSync(Buffer.concat([header,Buffer.from(result.data,'base64')]),{level:9});await fs.writeFile('public/textures/hall-environment.bin.gz',bytes);console.log({width:result.width,height:result.height,bytes:bytes.length,max:result.max});
}finally{await browser.close();}