# Local editor only — never commit real values.
# PUBLIC_EDITOR=1
# PUBLIC_EDITOR_PASSWORD=your-local-password

# Spielhalle: Basis-URL der Godot-Web-Builds in Produktion (R2-Bucket hinter eigener Domain).
# Leer lassen, dann lädt die Seite same-origin aus public/game-builds/<id>/ (nur lokal sinnvoll).
# PUBLIC_ARCADE_BASE=https://arcade.dennisbf.design

# Only enable IDs after exporting with the ready protocol and verifying the hosted build.
# PUBLIC_ARCADE_RELEASES=echo-frequency
