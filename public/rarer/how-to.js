import {tiers} from './core.js?v=b6103573572c';
import {iconSvg} from './icons.js?v=b6103573572c';
const gem='<svg viewBox="0 0 16 16" aria-hidden="true"><path fill="currentColor" d="M4 2h8v2h2v6h-2v2h-2v2H6v-2H4v-2H2V4h2z"/><path fill="var(--paper)" opacity=".6" d="M5 4h5v2H7v4H5z"/></svg>';
const ladder='<svg viewBox="0 0 24 28" aria-hidden="true"><path d="M5 2v24M19 2v24M5 6h14M5 13h14M5 20h14" fill="none" stroke="currentColor" stroke-width="3"/><path d="m9 5 3-3 3 3M12 2v11" fill="none" stroke="var(--paper)" stroke-width="2"/></svg>';
export function visualGuide({help=false,risk=true}={}){
 return `<div class="visual-guide${help?' guide-help':''}">
 <figure class="rarity-demo" aria-label="Every answer is ranked by how often people look it up. Each answer must beat the rank of your last one.">
 <div class="demo-label"><span>Ranked by how often it’s looked up</span><span class="demo-direction">Rarer ${iconSvg('ui-next')}</span></div>
 <ol class="demo-steps">${[['#3','Your first pick','Start common'],['#18','A little rarer','Beat #3'],['#41','Rarer again','Beat #18']].map(([rank,name,hint],i)=>`<li class="demo-step common" style="--step:${i}"><div class="demo-answer"><strong>${rank}</strong><span>${name}</span><small>${hint}</small></div><div class="demo-treasure">${gem.repeat(i+1)}</div>${i<2?`<span class="demo-arrow" aria-hidden="true">${iconSvg('ui-next')}</span>`:''}</li>`).join('')}</ol>
 <figcaption>Rarer = <strong>fewer people look it up.</strong> Beat your last rank.</figcaption>
 </figure><div class="tier-legend" aria-label="Six rarity tiers, from common to legendary">${tiers.map(t=>`<span class="${t.className}"><span class="tier-dot" aria-hidden="true"></span>${t.label}</span>`).join('')}</div>
 <div class="guide-choices">${risk?`<div class="guide-risk"><div class="spill-picture" aria-hidden="true">${gem}${iconSvg('ui-warning')}<b>−50%</b></div><strong>Slipped up?</strong><span>The first slip is a warning. The second caves in.</span><div class="three-misses" aria-label="The second slip ends the dig and spills half the cart"><i>!</i><i aria-hidden="true">${iconSvg('ui-warning')}</i><span>Cave-in</span></div></div>`:''}<div class="guide-bank"><div class="bank-picture" aria-hidden="true">${ladder}${gem}${gem}${gem}${iconSvg('ui-check')}</div><strong>Climb out anytime.</strong><span>Collect your haul and combos at home.</span><div class="bank-safe"><span class="combo-glyph">${iconSvg('ui-home')}</span> Home treasure stays safe</div></div></div>
 ${help?`<p class="guide-footnote">Just above your last rank is a close call: no penalty. Unknown and repeated answers are free. No timer.</p><details class="scoring-details"><summary>How points work</summary><p>Rarer finds are worth more, and a bigger leap from your last answer pays up to double. A cave-in keeps half your haul, but no combos.</p><p>Combos are collected when you get home, +25% each: <span class="combo-glyph">${iconSvg('combo-rainbow')}</span> Rainbow, one answer in every tier. <span class="combo-glyph">${iconSvg('combo-triple')}</span> Triple, three answers in one tier. <span class="combo-glyph">${iconSvg('combo-staircase')}</span> Staircase, four tiers up in a row. <span class="combo-glyph">${iconSvg('combo-legendary')}</span> Legendary, any legendary answer. <span class="combo-glyph">${iconSvg('combo-spotless')}</span> Spotless, six answers without a slip. All five together add +125%.</p><p>Three categories a day, shared by everyone. New digs at 00:00 UTC. Ranks use a year of Wikipedia pageviews.</p></details>`:''}
 </div>`;
}
