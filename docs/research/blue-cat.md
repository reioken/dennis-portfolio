# Blue — portfolio hall companion

## Identity and source

Blue is Dennis's black cat with amber eyes, confirmed by Dennis on 2026-09-12. The lighter cat in the supplied archive is a different cat.

Source archive: `drive-download-20260911T230121Z-1-001.zip`. Local originals and generation records are retained under `.source-assets/blue/`; original personal photographs are not published with the website.

Preserve broad cheeks, a short dark muzzle, black nose, dense short black coat, compact adult build, substantial paws and a thick tail. No invented white markings or collar. The stylized reference was generated with the built-in image-generation tool using the face and full-body photographs.

## Production checks

- First Meshy 7 Ultra single-image output: rejected, flattened segmented geometry (35 credits).
- Second Meshy 7 standard single-image output: rejected, extremely thin body and malformed anatomy (30 credits).
- Third attempt: multi-image reconstruction from front, side and three-quarter views succeeded (30 credits). Task `01a092ce-e72d-73f4-b9f5-bd9c4af497e9`. Total generation cost: 95 credits.
- No failed mesh is an acceptable fallback for the live website.

## Rigging research

Meshy's web app advertises quadruped rigging, but its public rigging API currently documents support for standard humanoids. Do not submit the cat to the humanoid API.

- [Meshy rigging API](https://docs.meshy.ai/en/api/rigging)
- [Meshy image-to-3D settings](https://docs.meshy.ai/en/api/image-to-3d)
- [Three.js animation blending](https://threejs.org/docs/pages/AnimationAction.html)
- [Quaternius Cat, CC0 motion reference](https://poly.pizza/m/qKICY6xla2)

The retained CC0 reference model is `.source-assets/blue/quaternius-cat.glb`. It is an animation reference, not Blue's appearance. Its simple four-leg rig is insufficient by itself for the desired expressive custom character.

## Behavior contract

Basket beside the left of the About claw cabinet. Restricted roaming path in front of that area, with pauses, a smooth settling pose, breathing while asleep and a wake-up. Clicking/tapping interrupts the routine with an affectionate head tilt, relaxed closed eyes and expressive tail movement. Keyboard access uses a projected focus target. Reduced-motion preference disables roaming. Scene pause must also pause Blue's clock.

The model belongs to the startup asset gate and must not pop into an already-lit room. Keep the texture budget modest, use opaque fur materials, avoid alpha hair cards and high-contrast emissive edges. Pick the actual cat mesh before processing cabinet/backdrop actions. Preserve the single persistent hall instance across internal navigation.

## Delivered locally

- `public/models/blue-rigged-v1.glb`: 1,961,872 bytes; 26 joints; six animation clips; facial blink and floor-contact corrective morph targets. Amber iris material factor preserves the generated pupil texture.
- `scripts/models/blender/blue_rig.py`: deterministic skinning, surface weight smoothing, two-segment leg solving, looped clips, pose-space contact correction and GLB export. Editable packed master retained in `.source-assets/blue/v3/blue-rigged-master.blend`.
- `src/components/hall/blueCat.ts`: interruptible behavior, cloth bed, contact shadow, nearest-hit interaction, mobile arrangement and accessible projected control.
- Cat and bed load before the lighting reveal; rendering pauses with the hall. No alpha fur cards or extra animation libraries.

## Verification

- TypeScript and production build passed; 27 existing regression tests passed.
- Built-site audit: 54 pages, local links and metadata passed.
- Chromium: 1440×900 and 390×844, plus reduced motion. Real pointer clicks and keyboard activation preserve the current route. Simulated two-minute cycles visit walk, idle, settle, sleep and wake within the restricted path; hidden/off-screen state does not advance its clock.
- Firefox 155.0.1: startup completes, mixer advances, actual pointer click triggers happy and closed eyes; accessible control has no hidden ancestor.
- Existing Firefox cube compositor regression: all six faces accelerated throughout two 9.6-second main-thread blocks; maximum sampled rotation hold 41 ms. Startup dismisses and reduced motion passes.
- Blender studio inspection includes idle, walking, settled, sleeping and happy poses. Contact correction keeps the sampled resting underside above the floor. These are authored stylized animations, not motion capture.

QA scripts: `scripts/qa/blue-cat.mjs`, `scripts/qa/blue-cat-firefox.mjs`. Screenshots and detailed results are retained in `.source-assets/blue/qa/`.

## v2 polish — 2026-09-12

Dennis's feedback on v1: not clean enough, not interactive enough, animations far from smooth. Diagnosis from the delivered v1 asset and code:

- The glTF exporter had decimated the sampled clips (some 4-second channels kept only 2–12 linear keys), so slow motion advanced in visible steps.
- The authored motion was tiny (idle head sway under one degree, paw lift 2.8 cm) and the sleep pose dropped the torso 11–13 cm although the belly sits 12 cm above the floor, which crushed the body and triggered the floor-contact morph across the whole underside.
- The Meshy atlas carries streaky fur highlights and warm specks that read as noise on a black coat.
- Runtime turning used a fixed angular rate, walking clamped the clip time scale (foot sliding at low speed), and clip switches were plain crossfades.

### Pipeline

`scripts/models/blender/blue_rig.py` is unchanged and still produces the skin master. The new `scripts/models/blender/blue_animate.py` opens that master and:

1. Rebuilds all six clips positions-first (torso chain, IK legs, tail chain, explicit head/ear rotations) at 30 fps: `idle` 8 s look-around with ear flicks and tail stir, `walk` 1.2 s lateral-sequence gait with 4 cm paw lift, paw pitch, body roll/yaw and head bob, `settle` 2.4 s into a **sphinx rest** (torso down 8.5–10 cm, front legs extended, hind legs tucked, tail along the flank), `sleep` 6 s breathing and head droop loop, `wake` 2.4 s reverse, `happy` 4 s head tilt, chin up, ears back, tail up.
2. Recomputes the `BlueGround` corrective for the sphinx pose.
3. Cleans the coat: the 4096² atlas is averaged to 1024², the amber eye polygons are rasterised through their UVs, the pink inner-ear polygons through the ear-bone weights, and everything else is replaced by a smoothed satin black. No colour-threshold survives, because warm highlight specks in the atlas map onto the face and chest.
4. Exports with `export_optimize_animation_size=False`, then repacks the GLB: channels that never leave the node's rest value (all scale channels, static translations) are dropped and the binary chunk is rebuilt. Sparse morph-target accessors are remapped as well; missing that made three.js fail with an invalid typed array length.

Result: `public/models/blue-rigged-v2.glb`, **2,144,196 bytes** (v1: 1,961,872), same 26 joints and two morphs, 322 static channels dropped, 30 fps keys on every remaining channel. `blue-rigged-v1.glb` is retained for rollback only.

Two texture lessons from this atlas: the polygons are only a few texels wide at 1024², so even a 2 px mask dilation paints neighbouring islands; and bright eye texels bleed into adjacent islands at coarse mip levels, which showed as glinting specks on the face and chest of the resting cat. Disabling mipmaps removed them but would shimmer; capping the eye texels at luminance 0.5, removing the dilation and anisotropy 8 leaves only faint residue.

`scripts/models/blender/blue_check.py` renders the authored poses and reports edge stretch per clip into `.source-assets/blue/v3/check-v2/`. Remaining stretch outliers sit at the wrists in the sphinx pose and between the front legs during the walk, both hidden from the hall camera.

### Runtime (`src/components/hall/blueCat.ts`)

- Idle and walk are one locomotion layer whose split follows the actual ground speed; the walk time scale equals speed ÷ 0.19 m/s, so feet never slide. Speed is acceleration-limited (0.28 up, 0.36 down) with braking distance to the goal; turns ease out into alignment. Stops are chosen randomly from five spots; after two to four visits Blue returns to the cushion and turns into a fixed resting heading so the lying pose reads from the frontal camera.
- Settle, sleep, wake and happy fade in over the locomotion layer. Petting a resting cat plays wake first, then happy.
- Attention overlay after the mixer: neck, head and ears follow the pointer (clamped ±0.75 rad yaw, −0.3…0.42 rad pitch, damped), lose interest six seconds after the pointer stops, and look at the viewer while the pointer rests on the cat, with perked ears, a pointer cursor and a slow blink. Without a pointer Blue glances at the camera at low weight. Reduced motion disables the overlay.
- Material: `MeshPhysicalMaterial` sheen (0.55, roughness 0.6, cool grey) gives the coat a velvet rim under the rect lights; eyes are glossy (roughness 0.32). Normal map strength 0.35, texture anisotropy 8.
- `hallScene.ts` passes the pointer ray and hover state through `look()` on every cursor update and clears it on `pointerleave`; loader errors are now logged instead of swallowed.

### Companion behaviour (same day, Dennis's follow-up)

Dennis asked for two things: on "Über mich" Blue should jump onto the claw machine, look down and lie there like a photo of him on a shelf edge with his front paws hanging over; on the other stations he should slowly walk past the arcades and sit in front of them.

- Five more clips in `blue_animate.py`: `sit` (1.8 s, stand → upright sit), `sitidle` (6 s loop with glances, ear flicks and tail-tip flicks), `jump` (1.4 s in place: crouch 0–0.30 s, launch, flight 0.42–1.10 s, landing crouch; the runtime moves the body), `perch` (2 s, stand → lying at a ledge with the front legs hanging below it) and `perchidle` (6 s loop). `stand` and `unperch` are `sit` and `perch` played backwards. Two more pose-space correctives, `BlueSit` and `BluePerch`, keep each underside on its surface; `BluePerch` skips the front-leg vertices because those are meant to hang. Morph normals are no longer exported (they cost 350 KB and change nothing visible). Asset: 2,260,396 bytes, 11 clips, four morphs.
- The hall now passes `{ focus, pose, stationX, inHall }` every frame and keeps Blue active at every station and behind open panels. `blueCat.ts` derives a plan from that: **home** (station 0 in the hall: the basket routine as before), **station n** (sit beside cabinet n) or **perch** (About: station 0 with a non-hall pose). Plan changes stand him up, wake him or climb him down first, then `resume()` continues from wherever he is.
- Station: he strolls at 0.34 m/s along a lane 0.8 m in front of the cabinets and sits 0.92 m beside the focused one, on the side he arrives from, facing the visitor; after 18–36 s he lies down (sphinx), 14–22 s later he sits up again. If the target is further than 3.4 m he is placed 2.6 m outside the frame on the approach side and walks in, so long hall jumps do not turn into 30-second treks.
- Perch: he walks to 1.05 m in front of the claw cabinet, crouches and leaps 1.95 m onto its top (a stylised cat leap with an eased rise and a small overshoot, nose up then down), turns to face the viewer, steps to the marquee's front edge (top surface 1.95 m, front face z 0.42 measured by raycast) and lies down with the paws hanging in front of the marquee. Petting on the ledge closes his eyes and pushes his head instead of standing him up. Leaving About plays `unperch`, a forward hop down to the floor, then the walk home.
- Reduced motion snaps to the plan's final pose without travelling. Beside an open panel only calm moods render every third frame.
- `scripts/qa/blue-companion.mjs` drives the whole scenario in Chromium at 1440×900 (plus reduced motion and 390×844): station sit position, perch position/heading/height, petting on the ledge, climb-down after `history.back()`, no console errors. Screenshots in `.source-assets/blue/qa/companion-*.png`.

### Verification

- Blender: pose renders and stretch report for all clips (`blue_check.py`).
- Chromium 1440×900 (Playwright, `scratchpad` evidence scripts): head direction follows the pointer left/right, hover perks ears with a pointer cursor, petting closes the eyes and keeps the route, walk peaks at 0.21 m/s with acceleration under 0.36 m/s² over 300 frames at 60 fps, settle → sleep → wake cycle, no console errors.
- `scripts/qa/blue-cat.mjs` passes at 1440×900, 390×844 and reduced motion; `npm run test:review` 27/27; typecheck.
- Not covered: native Firefox rerun, physical devices, Safari.

## v3 — eye forensics and ledge pose (2026-09-12, evening)

Dennis on the live companion: the eyes bulge upward when Blue jumps onto the machine, the legs hang far too low on the ledge, and turning is a robotic spin. The overhaul plan is `blue-animation-plan.md`; this section records the first two phases.

### Eyes: the correctives carried the blink

`scripts/models/blender/blue_forensics.py` audits the skin weights on the eye and socket polygons, the deltas of every shape key grouped by dominant bone, and renders the jump launch frames one morph at a time (`.source-assets/blue/v3/forensics-before/` and `forensics/`).

Findings on the v2 asset: eye and socket vertices are weighted 100 % to `Head` and every vertex sums to 1, so skinning was never the problem. But `BlueGround`, `BlueSit` and `BluePerch` each moved the same 1,772 head vertices as `BlueBlink`, with the identical 15 mm maximum on the eyes, and `BlueSit`/`BluePerch` additionally carried 50–120 mm deltas on the tail and hind legs. Cause: `Object.shape_key_add()` defaults to `from_mix=True`, so each corrective started as a copy of whatever mix was active (the blink at 1 and the previously created correctives). At runtime that meant: lying down or perching faded the eyes shut, petting on the ledge stacked the real blink on top for a 30 mm displacement, and the "before" render with all morphs at 1 shows the eyes as two spikes. The extra deltas also explain the poor stretch numbers of every resting pose.

Fix in `blue_animate.py`: all key values are zeroed, correctives are created with `from_mix=False`, the corrective uses the armature modifier's rule (missing weight stays at rest) and head, neck and ear vertices are excluded from every corrective. Result: no corrective touches an eye vertex; edges stretched over 3× fell from 364 → 104 (sleep), 340 → 78 (sit) and 1,039 → 79 (perch); the GLB lost 140 KB of sparse garbage (2,120,092 bytes). The eyeball rebuild from the plan is therefore deferred: at the hall's viewing size the flat eyes read fine once nothing deforms them.

### Ledge pose

The wrist target was an absolute 0.30 m drop. New pose (`EDGE_Y`, `FRONT_HANG`, `HANG_SIDE`): the marquee edge is 0.25 m in front of the body origin, the elbow rests on the top 3 cm behind the lip, the wrist crosses the edge 4 cm below it and the paws curl by 0.75/0.55 rad with a few millimetres of asymmetry, so the paws end about 7.5 cm below the top instead of 20 cm. Chest first, shoulders protract 1.2 cm toward the lip, front legs follow, neck relaxes after the chest, tail last. Per-leg offsets now apply after the pose target (they were no-ops at full amount before), so the perch idle stirs are visible. Runtime `LEDGE.spot` moved from z 0.20 to 0.17 so the cabinet front face lands at the authored edge. `blue_check.py` renders the perch on a ledge prop from two low camera angles that mimic the hall camera.

### Turning with steps (Phase 3)

Dennis: "when turning he just spins". The runtime rotated the body with `quaternion.rotateTowards` at up to 2.4 rad/s while idle or walk played, and settle/sit/perch kept spinning during their clips; stand and unperch were reversed playback.

Authoring (`blue_animate.py`): four turn-in-place clips, `turnL45`/`turnR45` (1.15 s) and `turnL90`/`turnR90` (1.7 s), built in the *turning frame*. The body yaws by a trapezoid profile (rate ramps up over 0.3 s, holds, ramps down, so the peak stays near 78°/s) around a pivot 3 cm ahead of the origin, while each paw keeps its world position until it steps; in body space a planted paw therefore sweeps backwards. The step schedule is simulated at 240 Hz: a paw steps when the body has yawed more than half a step (45°) past its planted angle, lands half a step ahead of the body's current yaw, only diagonal pairs may swing together, the paw furthest behind has priority, and the inner front paw starts. This keeps every paw within ~26° of the body (about 9 cm at the paw radius) instead of the 57° the first attempt produced. The head acquires the new direction first (0.34 rad lead, settling to zero), the chest leads and the pelvis lags by a few degrees, the chest rolls into the turn, the tail swings outward with lag. The Root bone carries the yaw curve.

Runtime (`blueCat.ts`): the Root tracks are stripped from the turn clips at load and sampled through a linear interpolant instead, so the skeleton plays in the turning frame and the body group is rotated by the same curve (`applyTurn`). A heading error above 0.35 rad while standing picks the clip whose nominal angle is nearest (45° stretched between 0.5 and 1.3, 90° between 0.65 and 1.25) and chains a second turn for the rest; `afterTurn` continues with the walk, the arrival clip or the jump. Walking yaw is limited by a 0.45 m turning radius (0.3–1 rad/s), a goal far off the heading brakes to a stop so a turn clip can take over, arrivals turn before settle/sit/perch, the jump no longer snaps its heading, the petted reaction only nudges the body when the visitor is within 0.7 rad, and while curving the chest rolls and the head leads along the path from the measured yaw rate. `stand` and `unperch` are authored (weight forward, hind legs push; one paw after the other back onto the ledge). Plan changes during a turn are deferred to its end. Clip lengths come from the loaded clips.

Regression: `scripts/qa/blue-turn.mjs` drives a goal straight behind Blue and the return to the basket frame by frame, and asserts that turn clips carried the 180°, that walking yaw stays under 1.15 rad/s, that a turn precedes the settle and that the settle itself no longer spins.

### Jumps with root motion (Phase 4)

The in-place `jump` clip and the scripted parabola in `fly()` described different accelerations, and the runtime pitched the whole model separately. Replaced by two authored clips whose Root bone carries the trajectory: `jumpup` (1.5 s: look up, load the hindquarters with the pelvis lower than the chest and the weight back, launch nose-up with the hind legs extending, arc with the front legs folded and the tail back, front paws land first, the hindquarters follow, absorb, settle; 1.95 m up and 1.03 m forward) and `jumpdown` (1.3 s: peer down, push off, accelerating drop nose-down, front paws take the landing, hind contact, settle; 1.95 m down and 0.88 m forward). Whole-body pitch is applied to the skeleton about a pivot at the chest, so it survives the Root stripping. At load the runtime removes the Root tracks, samples the displacement curve and replays it on the body, warping the forward and vertical components separately to the actual take-off and landing points (the compact layout scales Blue but not the cabinet), so the pose and the flight can no longer disagree and the landing is exact by construction. `blue_check.py` follows the Root with camera and lights and hides the floor while airborne.

### Polish round (Phase 5, late 2026-09-12)

Dennis after seeing v3 live: too much clipping, the bed must look far better, Blue should sleep there at the start and get up when the visitor does something, the cabinet jump must not pass through anything, turning and moving should be more realistic, touching different parts should get different reactions, and some turns still had no animation.

- *Jump paths.* Take-off moved to 0.80 m in front of the cabinet and the landing is the lying spot itself. `jumpup` now rises to 2.12 m before any forward travel, hooks the front paws over the marquee top and swings the body in; `jumpdown` pushes off forward first so the hindquarters clear the marquee, then drops. The runtime warps both per axis as before.
- *Bed.* Rounded-rectangle velvet bolster (superellipse, sheen material, woven bump, lavender piping along the ridge), quilted cushion that dips towards the middle, dark plinth. The bed is rotated to Blue's resting heading, its centre 6 cm behind his body origin, the front lip lowered so the sphinx paws and the head rest on it instead of passing through a ring (`BED` constants). Body lift on the cushion 0.085 m, ramped from 0.55 m out so the paws clear the lip on the way in.
- *Sleeping start.* `startAsleep()` puts him in the bed asleep with an infinite dwell; `disturb()` wakes him on 0.7 s of hover, a pet, or a plan change (station pick, About). After that his normal roam/sleep cycle runs. Reduced motion shows him asleep too.
- *Touch reactions.* `pet(hit)` finds the region by the nearest bone (Head/Neck → head, Tail2–5 → tail, else back). Head: the purr (lean in, slow blink; head push while seated or on the ledge). Back while standing: the new `arch` clip (spine up, hindquarters rise, tail up, head dips). Tail: the new `flick` clip stored additively against its own first frame and played on top of any pose (tail lash, ears back, glance over the shoulder, small flinch). Keyboard activation counts as a head touch.
- *Turns.* `TURN_MIN` 0.2 rad and the 45° clip stretchable down to 0.25, so nearly every standing turn is stepped; the settle residual eases at ≤ 0.25 rad/s; the petted reaction no longer rotates the body at all; after landing on the cabinet he turns to the viewer with steps before lying down.
- Not addressed: inertialized interrupts and tail/ear springs ("move more realistically" beyond the authored clips).

### Inertialized interrupts and secondary motion (Phase 6)

Dennis: "do that too" for the remaining "move more realistically" items. `blueCat.ts` now runs a post-mixer pose pipeline every frame (`pose()`): recover the mixer's value for every bone (bones a clip does not key keep last frame's mixer value rather than what the pipeline wrote), apply the inertial offset, run the springs, then the attention overlays, then remember what was written.

- *Inertialization.* Any transition that is not between two resting moods (settle, sleep, wake, sit, sitidle, perch, perchidle) now switches in one frame: the old action stops, the new one gets weight 1, the idle/walk pair snaps. On that frame the pipeline stores, per bone, the rotation from the new pose back to the pose shown last frame and the angular velocity of the shown pose (from its last two frames, capped at 12 rad/s), and decays both with Bollo's critically damped curve over 0.22 s (`decayCritical`). The earlier 0.06 s crossfade plus a slow offset had merely moved the pop into the fade frames; the mixer pose has to jump all at once for the offset to cover it.
- *Tail springs.* Tail1–Tail4 (Tail5 is a static end bone, its channel is pruned) follow the mixer's rotation through second-order springs in rotation-vector space, stiffness 420 → 110 towards the tip with damping ratios 0.75 → 0.55, two substeps at a clamped 1/30 s. The body's smoothed acceleration and measured yaw rate add a deflection request: forward acceleration pulls the tail back and down, turning swings it out (0.28 rad per rad/s at the base, fading to zero by Tail4), applied in the bone's own frame and rotated into the parent frame. Measured on a 180° turn: up to 0.27 rad deviation at Tail4, settling to 0.016 rad within two seconds of standing still.
- *Ears.* The same spring (k 520, ζ 0.5) on both ear bones gives their authored flicks a little overshoot; landing a jump adds an angular impulse that kicks them back once (`earKick`).
- Cost: ~0.045 ms per `update()` for the whole cat (26 bones), measured in Chromium.
- Reduced motion skips the inertializer and the springs; a pause longer than 0.2 s or a teleport resets the spring states and the body-velocity estimate.

### Asset budget (Phase 7)

The runtime GLB had grown to 2.36 MB: a 24k-vertex skin at full float precision, four sparse morph targets and dense 30 fps keys on every bone for 20 clips. The textures were already WebP and small (48 KB normal map, 40 KB coat). New packaging step `scripts/models/blue-pack.mjs` (glTF Transform API) runs after `blue_animate.py`: `resample` at a 1e-4 tolerance drops the keys the bake produced on straight segments, `prune` removes what nothing references, and `meshopt` at level medium quantizes positions and normals to 16 bit, texture coordinates to 16 bit and weights to 8 bit and compresses geometry, morphs and animation with `EXT_meshopt_compression`. Result 919 KB (39 %). The hall's `GLTFLoader` gets `setMeshoptDecoder` from three's bundled WASM decoder; the other models are untouched.

`scripts/qa/blue-asset.test.mjs` (part of `npm run test:review`) guards the file against an optimiser pass changing anything the runtime addresses by name: the 26 joints once each, the exact clip list, morph target names and order, the two materials, Root rotation only on turn clips and Root translation only on jump clips, turn end angles within 1.5° of nominal, jump displacements within 2 cm of authored, and a 1.6 MB size ceiling. KTX2/Basis textures were not adopted: at 88 KB the textures are not the cost, and the toolchain is not installed.

### Dynamic authoring pass (Phase 8)

Dennis after the motion round: "the turning animation still looks way too robotic, in general a lot of the animations look way too robotic and not dynamic like a real cat", plus: he wakes too slowly with the eyes shut too long, and he teleports when the visitor scrolls the arcades.

The cause was in the authoring, not the runtime: every clip was symmetric smoothstep segments on isolated parts with a rigid torso and metronomic steps. Changes in `blue_animate.py`:

- *Bendable spine.* A `bend` channel per torso segment curves the chain in yaw after the drop-based positions are computed (each segment turns a little further than the previous one), so the torso forms a C in turns and an S in the walk; planted paws keep their spots and the leg roots ride on the bent chain. A `root_dz` channel lifts a shoulder or hip with its swinging leg.
- *Easing library.* `ease_out`, `ease_in`, `ease_back` (overshoot and settle) and `curve()` (per-key ease names: smooth, out, in, back, snap) replace uniform smoothstep where weight arrives or a target is acquired; `jitter()` gives deterministic ±12 % variation to repeated timings.
- *Turn.* The head snaps to the new direction (`snap` ease over 0.16 s), the spine bends into a C behind it (up to ~0.5 rad across neck, chest and spine for 90°, half for 45°), the weight shifts onto the inside and rolls out, the chest rolls in, swinging paws arc outward and lift their shoulder, the tail whips out with a tip flick, both ears go back briefly. The root yaw profile now starts quickly and settles slowly (ramp in 0.22 s, out 0.5 s), swing durations are jittered.
- *Walk.* Hips and shoulders roll (0.06/0.04 rad) and the pelvis yaws, the spine sways in an S with the neck and head countering, the weight sits over the stance side, shoulders lift with each swing, paws reach fast and are set down decelerating (`1-(1-u)^2.4`), higher lift, occasional ear flicks.
- *Idle.* Weight drifts side to side with the spine following, and once per loop a front paw is lifted and re-planted.
- *Settle and sit.* The torso arrives with a small overshoot (`ease_back`), the front dips as it folds, weight rocks back before the hindquarters come down.
- *Wake.* 1.5 s instead of 2.4: head up first with a small shake and ear flicks, then the front pushes up, then the hindquarters. In the runtime the eyes open in the first half second of the wake and hover wakes him after 0.35 s.
- *Trot.* New two-beat diagonal gait (0.52 s cycle, 0.9 m/s nominal, bouncing back with the spine flexing, long reaching strides, tail carried up and back). The runtime blends walk into trot by speed (0.42–0.62 m/s) and trots to any goal further than 1.8 m at 0.9 m/s with stronger acceleration; the off-screen teleport for far stations is gone.

Stretch reports stayed in range for every clip (walk p99 1.52, turn frames 1.19–1.62). Asset after packing 943 KB.

### Hall models (Phase 9)

Dennis: optimise all the other 3D models too. Two scripts, run in this order: `scripts/models/shrink-textures.mjs` caps textures (base colour and emissive 2048², normal/roughness/occlusion 1024², all WebP at quality 86) and `scripts/models/pack-models.mjs` deduplicates buffers, prunes, resamples animations and Meshopt-compresses with quantization, in place, keeping originals in `.source-assets/models-original/`. The packer guards structure (node, mesh, material, animation and skin names unchanged, one unnamed wrapper node tolerated) because the hall addresses controls, screens and glow parts by name, and restores any file the pass would make larger. Kenney props are skipped (not loaded by the hall); Blue keeps his own packer.

Two traps: glTF Transform's `textureCompress` and the `functions` bundle pull in a second native sharp build, and two libvips in one process fail every encode with "colourspace: parameter space not set", so the texture pass lives in its own process; and sharp ignores a `Uint8Array`'s byte offset into the GLB buffer, so images are copied into a `Buffer` first.

Result, measured as what the hall actually downloads on a fresh visit: 13.0 MB → 6.1 MB over 19 files. Dennis figure 4.8 → 1.5 MB (its three 4096² maps were 268 MB of GPU memory; now 2048/1024/1024), Nori 2.0 → 0.6 MB, taxi 1.4 → 1.1 MB, Lowlight cabinet 0.54 → 0.19 MB, payphone 0.45 → 0.18 MB, the generated cabinets roughly halved. Before/after screenshots of the hall, About and a station are indistinguishable (`.source-assets/models-qa/`). The two versioned URLs (`dennis.glb?v=`, `mach-lowlight.glb?v=`) got new hashes.

### Coat (Phase 10)

Dennis: "the cat texture honestly looks really bad and low quality". Two causes in the v2 coat: the cleanup kept 35 % of the blurred Meshy atlas, whose island boundaries read as grey smudged patches on a black cat, and the untouched Meshy normal map added sparkle noise under the rect lights.

Now the albedo outside the eye and inner-ear islands is one even, slightly cool black (0.040/0.037/0.046), the material carries no normal texture at all (48 KB less in the file), and `blueCat.ts` generates a 256² tiling fur-grain normal at load (`furGrain()`: two octaves of hashed noise stretched along one axis, converted to normals from its gradients, repeated seven times over the atlas) at a low strength (0.32) with roughness 0.82, sheen 0.7 and a lavender-grey sheen colour. Close-ups on the ledge and in the bed at 2× (`.source-assets/blue/qa/coat-*.png`) show an even velvet black with rim sheen and no specks; a stronger grain (0.55) sparkled again and was backed off. Asset after packing 895 KB. The model's lumpy Meshy silhouette is the remaining quality ceiling; that needs a different mesh, not a texture.

### Face and eyes (Phase 11)

Dennis: "the texture in the face looks really bad still, not high res enough, mushy, not detailed, like broken". Two causes: the eyes were a few dozen blurry texels of the fragmented Meshy atlas with visible island edges, and dropping the Meshy normal map in the coat pass had also removed the sculpted nose, mouth and brow.

- *Eyeballs.* `blue_animate.py` measures each eye island (centroid, area-weighted normal), recesses those polygons 3 mm into the head as the socket floor and paints them black, and adds two smooth UV spheres (`Eye.L`/`Eye.R`, radius 16 mm, centre 9 mm behind the old surface so a ~2.6 cm disc shows) bone-parented to `Head`, local Z along the eye normal and local Y up. Their material `Blue amber eyeball` carries a generated 512² iris: amber gradient darkening outward, radial fibres, a dark limbal ring and a vertical elliptical pupil dilated for the dim hall. At runtime the eyeballs get clearcoat and low roughness, and sink 14 mm into the head as the blink morph closes the lids over them.
- *Face material.* Polygons whose vertices are dominated by Head, Neck or the ears (plus the old eye polygons) get `Blue face`, a copy of the coat material with a cleaned Meshy normal map: 4096² averaged to 2048², 3×3 median to kill the speck noise, softened 30 % towards flat. The runtime keeps that map at strength 0.85 on the face and applies the tiled fur grain only to the body.
- The structure guard now checks the three materials, that both eyeballs are mesh nodes under the Head bone, and finds the skinned mesh by its morph targets. Packed asset 1.03 MB (116 KB of it the face normal).
- First two attempts read as slits (sockets not recessed, sphere too deep) and then as glowing beads (too small, too bright, hairline pupil); the numbers above are the third.

### Stepped turns, cute eyes, black ear backs (Phase 12)

Dennis after the face round: the eyes were worse ("he needs his eyelids and big black eyes"), he still "just spins" when turning, animations still bad in general, and the ears looked skin-coloured from the outside.

A frame strip of the live runtime (`.source-assets/blue/qa/turn-strip.png`, 150 ms apart) showed why the schedule-driven turn read as a turntable: the body rotated at a steady rate the whole time while the paws took small, low steps. Replaced with explicit choreography (`turn_plan`/`turn_yaw`/`turn_paw`): the body rotates only while a diagonal pair of paws is in the air, in two beats for 45° and four for 90°, each swing 0.26 s with 5.5/4.5 cm lifts and outward arcs, a pause of about 0.11 s between beats, the first pair landing a beat's angle ahead of the body, a dip on every landing and the weight rocking onto the planted diagonal, timings jittered. The head snap, C-bend and tail whip stay. Clips are now 1.33 s (45°) and 2.07 s (90°). The strip afterwards shows the yaw advancing in steps with pauses and the paws clearly lifting.

Eyes: sphere radius 18.5 mm, centre 10.5 mm behind the old surface, pupil a large soft ellipse (0.58 × 0.66 rad of the cap) so the eye reads as big and dark with an amber rim; only the middle of the old eye island is recessed (falling off between 9 and 15 mm from the centre), so the outer ring stays as eyelids framing the eyeball; the eyeball retracts 17 mm on a blink. Ear backs: ear-weighted polygons not facing forward get `Blue ear back`, a plain black material without any texture, because the outside of the ear shares atlas texels with the pink inside. Packed asset 1.04 MB.

### Retime round after the live analysis (Phase 13, 2026-09-13)

`docs/research/blue-live-analysis-2026-09-13.md` measured the live cat at 60 fps and found the procedural clips two to five times off real timings, plus two defects. Dennis's decisions: no paid assets or commissions (Meshy credits allowed), retime first, keep the jump onto the claw machine, wake him by clicking him, big round pupils. This round, all in `blue_animate.py` and `blueCat.ts`:

- *Eyes.* The runtime looked up `Eye.L`/`Eye.R`; `GLTFLoader` sanitizes node names and strips the dots (`EyeL`), so `this.eyeballs` had always been empty and the eyes never closed on the site. Both spellings are tried now. Real lids: four spherical caps (`Lid.U.L`, `Lid.D.L`, …, 64×48 spheres, radius 18.5 + 1.4 / 0.8 mm) bone-parented to Head with the eyeball's own `matrix_basis` (assigning `matrix_world` after bone-parenting evaluated the bone at a different depsgraph state and put the caps 12 mm too high), tipped back 12° / 6° when open. The runtime hinges them by `blink` × 1.30 / 0.72 rad about the eye's lateral axis. The packer's quantization recentres each mesh and moves the offset onto the node, so a cap's node origin is its own bounding-box centre; the lid is therefore rotated rigidly about the eyeball's rest position (rest · hinge · rest⁻¹ applied to the offset), not about its node. The iris pupil is round (0.50 rad half-axes instead of 0.58 × 0.66) with a painted catchlight near the top. Eyeball retraction on a blink is 6 mm.
- *Turn.* One continuous sweep (`smooth(u^0.85)` over 0.42 s for 45°, 0.62 s for 90°, 0.10 s lead, 0.12 s settle) with two or three quick diagonal steps during it, the head snapping ahead. Clips are 0.64 s / 0.84 s. The runtime warps the 45° clip between 0.12 and 1.35 of its angle and the 90° clip between 0.6 and 1.5, plays it at `1/√scale` (clamped 0.85–2) and turns for anything above 4.6° (`TURN_MIN` .08). A 129° turn now takes 0.7 s (`dev-trace.json`) and the 180° regression carries it in two clips within 1.6 s at a 4.7 rad/s peak (`blue-turn.mjs` ceiling raised to 6.5).
- *Walk.* `STRIDE_SPEED` .34 over a 1.1 s cycle (37 cm strides, ≈0.9 strides/s at 0.34 m/s; the roam at 0.21 m/s plays it at 0.62 as a prowl), the head with its own 2 cm bob, the tail carried low with a lazy wave instead of the hook. `FAR_DISTANCE` 1.5 so the walk to the take-off is a trot.
- *Sit, stand, settle.* 0.9 s, 0.9 s and 1.6 s; the forepaws step in under the chest during the sit, one a little ahead of the other, the head slightly off axis. A new `sitarch` clip (1.6 s) answers a stroke along the back while seated; head and ledge touches push the head up 0.28 rad and roll it 0.18 with the chest leaning along.
- *Jumps.* `jumpup` (1.7 s): crouch to 0.40, launch, ballistic flight from 38 cm in front of the cabinet with 5.7 m/s vertical velocity to a hook 33 cm below the top, chest hauled 10 cm above the top while the hind legs scramble, forward over the lip, landing crouch. The nose-up pitch starts after the hind paws have left the floor (an earlier version pitched about the chest while loaded and pushed the rear 6 cm through the ground). `jumpdown` (1.5 s): peer, hop off at 0.35 m/s, free fall, forepaws touch with the body still 12 cm up and pitched 0.55 rad nose-down, the last 12 cm are the legs flexing (chest 10 cm down at the deepest point), hind paws a beat later. Everything is authored with `curve()` easings, no smoothstep-only `track()` keys, so the flight has no zero-velocity hitches. On desktop About the rise in front of the glass, the hook and the pull-up are now in frame from 8 s after the click instead of nothing until 10 s.
- Verification: `blue-asset.test.mjs` (now also guards the four lids), `blue-cat.mjs`, `blue-companion.mjs`, `blue-turn.mjs`, 33 review tests, typecheck; dev-server 60 fps strips of the whole loop (`.source-assets/blue/qa/live-analysis-2026-09-13/` holds the before, the scratchpad probes `live-strip.mjs`/`restrip.mjs`/`dense.mjs`/`dev-trace.mjs`/`dev-head.mjs` the pattern); head close-ups at blink 0 / 0.5 / 1 on the dev server.
- Known limits: the lid cap edge is polygonal at 2× (visible only during a blink in the About close-up), the perch clip still spends its first half second standing, hind paws rise about 30 cm above the top during the pull-up scramble, stretch p99 on the hook and fall frames is 2.7 (legs fully extended).

### v4: a new mesh from Meshy (Phase 14, 2026-09-13)

Dennis on the retime round: the eyes were still creepy, the lids seemed to move the whole eyeball; make a new model with Meshy and Blender, higher quality throughout, no paid assets or commissions, credits allowed.

- *References.* Meshy's image-to-image endpoint with `generate_multi_view` turned the existing stylised reference (`.source-assets/blue/reference/blue-standing-v1.png`) into a front/side/back turnaround; two image models were tried (`gpt-image-2`, `nano-banana-pro`, 12 and 9 credits). Dennis chose the `gpt-image-2` set ("B"). A text-to-3D candidate (25 + 10 credits, `/openapi/v2/text-to-3d`) came out as a generic kitten and was dropped.
- *Mesh.* `multi-image-to-3d` with the three turnaround views plus the reference as the fourth image, `meshy-7` ultra, textured 4k PBR, quad remesh at 80,000 polygons, texture prompt for the black coat and amber eyes (about 40 credits per run; the "A" set was run too and discarded). Task records and downloads live in `.source-assets/blue/v4/` (`generate.py`, per-stage `state-*.json`, `mesh-gpt.glb`, `mesh-gpt-pre.glb`). Result: 75k vertices after welding, one PBR material, sculpted fur clumps, a proper muzzle, nose, whisker pads, toes, amber eyes painted into the coat.
- *Normalisation.* `scripts/models/blender/blue_v4_inspect.py` rotates the longest horizontal axis to Y with the head at −Y, scales the body height to 0.42 m, drops the paws onto z = 0, renders orthographic views on a 5 cm grid, slices the mesh at eight heights (paw clusters at floor level) and writes `normalised.glb` plus a report. Blender resolves relative render paths against the drive root: pass absolute paths.
- *Rig.* `scripts/models/blender/blue_v4_build.py` builds the same 26-joint skeleton the clips are authored for. Joint positions come from `rig-config-gpt.json` (hand-tuned from the see-through joint renders, using the old rig's height ratios: torso at 0.60–0.63 H, shoulders/hips at 0.54–0.56 H, elbow/hock at 0.27 H, the tail following the mesh's hanging curl); an auto-proposal from the paw clusters is written when no config is given. Skin weights are capsule-distance falloffs per bone (radius by part: torso 11 cm, legs 4.5 cm, tail 3.5 cm), legs restricted to their side and below the torso line, then 14 rounds of surface diffusion and the best four influences. The armature modifier, `BlueBlink` morph and the master `.blend` come out of it.
- *Eyes without geometry.* The iris vertices are found by luminance in the base colour at the vertex UVs (bright, not pink, upper front of the head), clustered around the median. `BlueBlink` squashes that region toward the eye's horizontal midline. The closed look comes from a texture swap: `paint-closed2.mjs` repaints every amber or pale texel of the atlas (dilated 6 px) with the median fur colour and writes `public/models/blue-v4-closed.webp` (2048²); the runtime loads it and swaps the coat's base colour while `blink` > 0.5. Painting per triangle through the mesh UVs did not reach the iris texels (the eye-region vertices sample no amber in either V orientation, cause not established), and Blender's float pixel round-trip crushes the dark fur to flat black, so the atlas is edited as 8-bit in Node. No eyeballs, no lid caps: nothing separate moves in the face.
- *Clips.* `blue_animate.py` gained a v4 mode (`BLUE_V4=1 BLUE_MASTER=<master.blend>`): the legacy coat cleanup, face material, eyeballs and lids are skipped; the Meshy PBR material is renamed `Blue coat v4`, the base colour scaled to 2048², and the same 22 clips, correctives and export run unchanged on the new joints. `blue_check.py` renders every pose on the new mesh; the sheet looked right for all clips. Stretch p99 numbers are higher than before (2.3–3.2 on resting poses) because of the fur-clump faces, without visible tearing.
- *Runtime.* `hallScene.ts` loads `/models/blue-rigged-v4.glb` (Meshopt, 2.9 MB; the guard's ceiling is 3.4 MB for v4). `blueCat.ts` routes a `v4` material by name (keeps its own normal map at 0.7, no fur grain), holds the open and closed base colours, swaps on the blink, and anchors the click target lower when he lies (0.15 m) or sits (0.22 m) so a pointer resting on it also hits the mesh. `blue-asset.test.mjs` checks the v4 material and maps instead of the eye nodes.
- *Kept for rollback:* `public/models/blue-rigged-v2.glb` and the v2 pipeline paths.

### v5: stylised Pixar-style mesh, fur retexture, round pupils (Phase 15, 2026-09-13)

Dennis on v4: the 75k mesh was too heavy for the site, it looked like clay with the fur sculpted into the geometry; stylise it, generate new images first, then a new model, keep the site fast, keep the walk. Two more asks in the same round: a click on a cabinet should have him walk over and sit in front of it, and on the claw machine only the paws should hang over the edge, not the forearms. On the first stylised result: it looked like plastic, the fur texture was missing, and the tail was broken in many frames.

- *Style sheets.* `generate.py style NAME` (local, `.source-assets/blue/v4/`, copied to `scripts/models/blue-v4-generate.py`) runs `image-to-image` with `generate_multi_view` on the chosen set-B turnaround (12 credits per sheet). Seven styles were drawn on two contact sheets (`style-sheet.jpg`: `film`, `toy`, `game`; `style-sheet-2.jpg`: `dreamworks`, `pixar`, `pixar-cute`, `pixar-true`; 84 credits). The toy look read as plastic; Dennis chose number 4 of the second sheet, `pixar` (British Shorthair build, round head, big round amber eyes, velvet coat).
- *Mesh.* `multi-image-to-3d` from the three `pixar` views, `MESHY_POLYS=25000 MESHY_ULTRA=0`, quad remesh, 2k PBR (`mesh-pixar.glb`, `state-mesh-pixar.json`). 22,537 vertices after normalisation, one material. The texture prompt for that run asked for "no visible individual hairs", which is why it came out as plastic.
- *Fur retexture.* `generate.py retexture pixar` calls Meshy's `retexture` endpoint on the mesh task with `enable_original_uv=True` (10 credits): same geometry and UVs, new PBR maps painted from a fur prompt (dense short black fur, visible fine hairs, direction flow, lighter tips, pink ears, amber eyes). Result `retex-pixar.glb`, normalised into `inspect-pixarfur/`, built into `build-pixarfur/`.
- *Tail rig.* The `pixar` mesh carries the tail along the floor behind the body instead of the v4 hanging curl, so the v4 tail joints sat in empty space and the tail mesh followed whichever body bone was nearest, which is the tearing Dennis saw. `rig-config-pixarfur.json` puts Tail0–Tail5 on the measured centreline (y 0.17 → 0.35, z 0.09 → 0.068) with a 4.5 cm capsule; the rest of the joints use the v4 height ratios on the new proportions.
- *Corrective guard.* `blue_animate.py`'s ground/sit/perch correctives lift vertices along the inverse skin matrix; on the stylised mesh a few vertices near-singular under the sit pose produced lifts of metres and the sit exploded. Lifts over 0.3 m or a skin matrix with |det| < 1e-4 are skipped now (a first 6 cm threshold also dropped legitimate belly lifts, minz −0.15).
- *Eyes.* The build's iris detection took the pale inner-ear highlights of the fur atlas for eyes (luminance alone, ears within the search band), so `BlueBlink` squashed the ears and the first pupil pass painted them. Detection now requires an amber hue (red over green, blue under half the red), a band below 0.86 H, and mirrors the other eye when one side has fewer than six texels. Found at (±0.030, −0.325, 0.336), radius 13 mm. `scripts/models/blue-v5-pupils.mjs` paints the big round pupils Dennis asked for in mesh space: every atlas triangle around an eye is rasterised in UV with its 3D position interpolated per texel, and a texel goes dark when its surface point is within 6.6 mm of the eye centre (0.8 mm soft edge), with a 1.9 mm catchlight 2.4 mm nose-wards and 2.8 mm up. Round on the mesh whatever the UV layout does, no seam handling. It writes `blue-rigged-v5-pupils.glb` next to the clean export (the source is never overwritten; an earlier version wrote back into it and had to be re-exported twice). `blue-v4-closed-eyes.mjs pixarfur v5` keys the closed atlas from the fur base map, so the pupils do not affect the closed look.
- *Material.* `blueCat.ts` routes any `Blue coat vN` material. The plastic look at the About close-up had two causes: Meshy's metallic-roughness map sits around 0.55 and multiplies the roughness factor, so the coat rendered at about 0.5 roughness with broad environment reflections, and the Meshy normal map is flat (std 1.1 of 255), so nothing broke the highlights up. Now the roughness and metalness maps are dropped (roughness 0.94, envMapIntensity 0.3), the site's fur-grain normal is tiled 14× at 0.9 strength, sheen 0.55 at roughness 0.85 for a soft velvet rim instead of a latex band, specular 0.08. The fur strands themselves are in the retextured base colour.
- *Sit in front of the cabinet.* `STATION_SIDE_X` 0.12 instead of the old side offset: the station spot is on the lane in front of the clicked cabinet; the companion regression asserts the offset.
- *Paws over the ledge.* The perch corrective's `FRONT_HANG` hook target is pulled back 4 cm and up 2.5 cm (0.062, −0.262, −0.015 instead of 0.062, −0.30, −0.04) and the paw pitch raised to 0.8 / 0.62 per side, so the forearms stay on the top of the claw machine and only the paws turn down over the edge.
- *Asset.* `public/models/blue-rigged-v5.glb`, Meshopt, 1.18 MB (v4 was 2.9 MB), `public/models/blue-v5-closed.webp`; `hallScene.ts` loads v5; the guard checks `Blue coat v5` and the 3.4 MB ceiling. `blue-rigged-v4.glb` and `v2` stay for rollback.
- *Verification.* `blue-asset.test.mjs` (6 checks on the v5 pack), `blue-turn.mjs` (180° in two clips, 4.69 rad/s peak), `blue-cat.mjs`, `blue-companion.mjs` (sit offset 0.12, perch, ledge petting, climb-down, phone), 33 review tests, `tsc --noEmit`; dev-server 60 fps strips of the whole loop before and after the matte coat (`.source-assets/blue/qa/live-analysis-2026-09-13/desktop-dev-v5/`: wake, roam, station, head and body touches, About climb, ledge touch, climb-down) and head/body close-ups at blink 0 / 0.5 / 1 / −0.7 (`eyes/`). Looked at as an animator this time: the tail stays inside the mesh through the walk and the sit, the sit lands on the lane in front of the cabinet facing the room, only the paws hang over the marquee, the coat reads as matte fur in the About and climb-down close-ups and he stays readable from the hall camera.
- Known limits: the pull-up scramble seen from the About camera (7.6–8.6 s after the click) is a rear view of a blob with legs; the closed-eye disc has a faint seam and no lid line; the `pixar` mesh carries its tail low along the floor, so the walk has no tail-up; `blue_check.py` was not run on v5.

### v6: back to the first cat (Phase 16, 2026-09-13)

Dennis on v5 live: "EWWW no", and asked to see every cat model so far, "the first one was the best still I think". A comparison sheet of all nine meshes (`.source-assets/blue/qa/model-comparison-2026-09-13/blue-models-sheet.png`: same studio light, 0.42 m, rest pose on the idle clip, five yaws, rendered by `compare.html` + `compare.mjs` in the same folder) made the choice clear: "Use the one at the top. It was our best attempt." That is the v1 cat as it shipped on 2026-09-11: the Meshy mesh built from Blue's photos with its original atlas and the amber-tinted eye polygons, before the coat cleanup, the velvet, the eyeballs and the lids.

- *What v6 is.* The v1 mesh and rig (`blue-rigged-master.blend`, 29,680 faces, `Material_0` + `Blue amber eyes`, `BlueBlink` and `BlueGround` shape keys) exported by `blue_animate.py` in a new `BLUE_ORIGINAL=1` mode: all 22 clips, the three correctives and the Root motion of the current pipeline, but none of the legacy coat work. `Material_0` becomes `Blue coat v6` with the original atlas at 2048², the eye polygons keep `Blue amber eyes` with the baseColorFactor tint (1, .58, .18), the flat Meshy normal map is dropped. Packed: `public/models/blue-rigged-v6.glb`, 1.67 MB.
- *Blink.* Texture swap like v4/v5, but the eye region comes from the mesh, not from colour: `scripts/models/blue-v6-closed-eyes.mjs` rasterises the `Blue amber eyes` triangles in UV, dilates 3 px, paints the median fur colour of the surrounding ring (a quarter of the original texel kept so it is not a flat disc) and a dark seam across the middle third of the eye height as the lid line. `public/models/blue-v6-closed.webp`. The runtime swaps the atlas on both materials and lifts the eye material's amber tint to white while closed, so the painted lids stay fur-coloured; the `BlueBlink` squash still runs on top.
- *Runtime.* `blueCat.ts` detects the version from the coat name before routing (`original` = `Blue coat v6`) and lights every material the way v1's runtime did: roughness .88, environment .7, no normal map, plus a faint sheen (.25 at .8) so he still reads in the dark hall; the eye polygons are not made glossy. `hallScene.ts` loads v6. `blue-asset.test.mjs` expects `['Blue coat v6', 'Blue amber eyes']`, the shared atlas, the tint and no normal map.
- *Comparison sheet lessons.* Packed Blue files export the three correctives at weight 1 (the runtime resets them every frame); any standalone viewer must zero `morphTargetInfluences` or the tail fans out. The raw Meshy sources are z-up and untextured in the 55 MB master; use the cleaned exports.
- *Verification.* `blue-asset.test.mjs` (v6 expectations), `blue-turn.mjs` (180° in two clips, 4.69 rad/s peak), `blue-cat.mjs`, `blue-companion.mjs`, 33 review tests, `tsc --noEmit`; dev-server 60 fps strips of the whole loop (`.source-assets/blue/qa/live-analysis-2026-09-13/desktop-dev-v6/`) and head/body close-ups at blink 0 / 0.5 / 1 / −0.7 (`eyes/`): the original atlas reads as fur at the About and climb-down close-ups, the amber eyes with their painted pupils and catchlights are the v1 ones, the closed state is a fur-coloured patch with a seam, he sits in front of the cabinet and hangs only the paws over the marquee. The closed atlas is 1024² (249 KB; 2048² was 806 KB).
- Known limits: the closed-eye patch has no lid shape, the v1 atlas keeps its baked highlight specks (that is the look he chose), the perch clip still stands for its first half second, `blue_check.py` has not been run through the current script on v6.

### Curled sleep and a mapped way in and out of the bed (Phase 17, 2026-09-13)

Dennis on v6 live: "He needs more of a proper sleeping position. And he clips through a lot of things when he walks, like the bed. He needs a proper curled up sleeping position and mapped out when walking out the cat bed, so there's no clipping." Close-up strips of the v6 recording (`.source-assets/blue/qa/live-analysis-2026-09-13/desktop-dev-v6/`, `bedexit.mjs` crops) showed both: the sleep was the sphinx rest with a drooping head, and on leaving he walked straight through the bolster in whatever direction the next spot lay, his height merely faded down with distance from HOME.

- *Curled sleep.* `apply_curl()` in `blue_animate.py` on top of the sphinx rest: the spine bends sideways segment by segment (`CURL_BEND`, Spine .38 → Head .55 rad, about 120° in all, head to the cat's right so that from the resting heading the face is towards the hall camera), the body rolls a little onto the inside of the curl (`CURL_ROLL`), neck and head drop further (`CURL_DROP`), the forepaws draw in under the chest (`FRONT_CURL`), the head pitches down onto them with the cheek turned in, and the tail leaves the rump around the outside of the curl and lies along the flank towards the nose (`TAIL_CURL`, a new `tail_curl` channel in `solve()`). `settle` is 2.2 s now: the sphinx by 1.6 s, the curl over 1.05–2.15 s, head last; `sleep` breathes in the curl; `wake` uncurls in its first 0.6 s before the push-up. The `BlueGround` corrective is derived from the curled pose as before.
- *In and out of the bed.* Two root-motion clips, `bedout` and `bedin` (2.2 s each), built like the jump clips: the Root carries the body 0.55 m forward and 0.085 m down (or up). The footfalls are not hand-timed but derived from the root's travel (`footfalls()`): a paw swings for 0.32 s and lands half a stride (0.23 m) ahead of its neutral place under the moved root, stands until the body has travelled a stride, swings again, and the last swing lands at neutral so the clip ends in the rest stance; the four paws start in lateral sequence (LH, LF, RH, RF). A swing that crosses the lip is lifted 12–13 cm, no paw is ever planted within 5 cm of the lip's centreline, and the body descends (0.30–1.00 s) or rises (0.80–1.50 s) while the front is already on the other level, with a small nose-down / nose-up pitch. A first hand-timed version held planted paws too long, the leg IK dragged them and a hind paw could be pulled across the lip. Geometry from the runtime bed: cushion 8.5 cm above the floor, the lowered front lip 31 cm ahead of the resting body origin (`BED.z` .30 now, so the standing forepaws clear it) with its top 1.5 cm above the cushion.
- *Runtime.* `BED_EXIT` = HOME + 0.55 m along the resting heading, on the floor just outside the lip. `inBed` state; `leaveBed()` turns him to the resting heading if needed and plays `bedout` through the jump machinery (`startJump` takes a clip name, `afterJump` continues with whatever was planned: the roam spot, a station, the About take-off); `enterBed()` plays `bedin` from `BED_EXIT` to HOME on the cushion; arriving at `BED_EXIT` he faces the bed (`REST_YAW` + π), steps in facing the back wall, then turns round on the cushion (arrival `settle`) and settles facing the lip, the way a cat circles before lying down. The first version turned him to the resting heading at the waypoint, the jump machinery turned him back, and he lay down facing the wall. Every way home ends at `BED_EXIT` (`startWalk`, `resume`), the home routine stays in the bed after a wake until its idle dwell is over, the distance-based height fade is gone (`body.position.y = elevation`), the reduced-motion snap puts him on the cushion. `extractRootMove` handles `bed*` clips; the guard expects the two clips with their Root translation (0.55 m, ±0.085 m).
- *Check renders.* `blue_check.py` takes the v6 blend and gained shots for the sleep from four angles (incl. a top view), the end of the settle, the start of the wake and six frames each of `bedout`/`bedin`; `checksheet.mjs` (scratchpad) tiles them. Looked at before the runtime.
- *Verification.* Blender check sheets of the curl from four angles and six frames of each bed clip (`check-v6/`, p99 stretch 3.0 in the curl, 1.5–2.4 in the bed clips), then on the dev server: `dev-bed.mjs` close-ups from the front and from the side (2.3 m out) stepping the runtime by hand through wake → bedout → walk → bedin → turn → settle → sleep (`.source-assets/blue/qa/live-analysis-2026-09-13/bed/`), 60 fps strips of the whole loop from the hall camera (`desktop-dev-v6d/`), the turn, cat and companion regressions (the turn test's home scenario now ends at `BED_EXIT`), 33 review tests, typecheck. Looked at frame by frame: the front paws step over the lip and down before the body follows, the hind paws lift clear of the bolster, nothing passes through it; the wake uncurls before the push-up; from the hall camera the sleeping cat is a compact curl with the face towards the room.
- Known limits: the curl is at the mesh's deformation limit (worst stretch at the foreleg; tighter needs a corrective), the front paws hover a few centimetres above the floor for a moment while the body is still on the cushion (no runtime foot IK), the turn at `BED_EXIT` before stepping in takes a second, the roam spots are not checked against the bed footprint beyond their fixed positions.

### A cat-shaped curl, a clean atlas, dark ear rims (Phase 18, 2026-09-13)

Dennis on the Phase 17 deployment, with a photo of a cat curled up on its side: "He doesn't lay down naturally like a cat would. He should lay in such a position or something. Also, he has white pixel flickering in his face in some parts, and his ears need to be dark on the outside as well. His are currently a bit skin coloured there." Evidence and scripts: `.source-assets/blue/qa/curl-atlas-2026-09-13/`.

- *The curl, positions-first.* The Phase 17 curl was the sphinx rest with the neck bent round; it read as a cat looking over its shoulder. `apply_curl()` now places the torso joints on an arc on the cushion (`CURL_R` 13.5 cm from `CURL_CENTRE`, 187° from `CURL_START`, so the nose reaches the hind legs; `torso_curve` in `solve()` lerps the chain onto it by `curl_amount`, breathing on top, then restores the segment lengths from the pelvis), rolls the body onto the inside of the curl (`CURL_ROLL`, 77° at pelvis and spine, 60° at the chest, 46° at the neck; his right side, towards the hall camera), lays the head level on the forepaws with the face towards the room (`head_local`: rotations about the head's own axes, because the world-axis head channels mean nothing once the head has turned 180°), folds the hind legs into the curl and the forepaws under the chin (`paw_world`: world targets blended in by `curl_amount`) and lies the tail along the ground across the opening of the C, hugging the belly, to under the chin (`tail_world`; Tail0 stays on the pelvis). Footprint 37 × 44 cm, inside the cushion. Iterated in a pose lab (`blue_pose_lab.py`: exec of `blue_animate.py` up to the bake, an override file, one pose rendered from six cameras with the cushion footprint drawn, joint positions, the direction each torso bone's underside points, stretch; about 20 s per candidate instead of a 3-minute bake): A rolled onto the wrong side and folded, B was a ring with a hollow and the head face-down, C compact but with the nose buried, D1 (head roll −.4, pitch −.4 in its own frame) is what shipped; D2 (the mirror roll) put an ear on the ground. p99 stretch 3.9 (Phase 17: 3.0), worst at the shoulder under the neck; invisible at hall size, a fold in a 2× crop.
- *The flicker, measured.* `dev-flicker.mjs` hand-steps the runtime at 60 Hz, captures 36 consecutive frames of a crop and counts pixels that jump more than 45 luminance above their temporal median on a dark surface, with a heat map and the pixels marked on a frame. Hall idle, 240×180 crop of the head: 132 such pixels, 7–28 per frame, on the forehead, the cheeks and between the ears; station zoom 480×360: 569. With the atlas replaced by a flat colour: 0. With environment, specular and sheen off but the atlas on: 30. So the texture, not the lighting, and not anisotropy (1/2/8 alike). The Meshy atlas is 1,469 UV islands with 33 % gutter texels; at hall size the head samples mip levels 4–6, where the GPU's box filter has averaged 16–64 texels of several islands into each texel: eye whites, ear pink and whisker strokes bleed into whichever islands are their neighbours in UV space (random body parts), and the blend shifts with every sub-pixel move of the head.
- *The fix: island-aware textures* (`scripts/models/blue-v6-atlas.mjs`, replaces `blue-v6-closed-eyes.mjs`; runs on the Blender export, before the packer). The eye polygons are a separate primitive, so the eyes get their own textures: 1024² open (the eye islands on a fill of the eyes' mean colour, so their GPU mip chain stays clean) and 512² closed (fur with the lid seam), and nothing bright is left in the coat atlas. In the coat atlas the eye polygons plus a 3 px rim are painted with fur, the gutters are padded with the nearest island's colour, and the mip chain is built island by island: each coarser texel is the majority island of its 2×2 block and averages only that island's texels; at 128² and below the inner-ear islands go to the coat colour, at 256² and below nothing brighter than 110 (70 at 128²) survives. The 1024² level is the GLB's coat texture, the 512²..1² levels are stacked in one 512-wide column (`public/models/blue-v6b-mips.webp`, 89 KB) that the runtime slices into ImageBitmaps and uploads as the texture's mip levels (`BlueCat.attachMipSheet`, `texture.mipmaps`, `generateMipmaps = false`); the blink now swaps only the eye texture and the hall waits for `blue.ready` (closed-eye texture and sheet) before the reveal. The three files are named after an asset build (`BLUE_ASSET_BUILD` = `v6b`: `blue-rigged-v6b.glb`, `blue-v6b-closed.webp`, `blue-v6b-mips.webp`), not after the material version any more: the first upload under the old names showed why. `/models/*` has been edge-cached for a day since the performance pass, and `www.dennisbf.design` kept serving the previous 249 KB `blue-v6-closed.webp` next to the new GLB; an old GLB next to the new 2 KB closed texture would have swapped the whole coat for it on every blink. Any content change under `/models/` or `/textures/` gets a new file name from now on. Result on the same probe: 132 → 23 sparkle pixels (the residue is the two eye polygons and silhouette motion), the GLB 1.71 → 1.16 MB, the closed-eye file 249 KB → 2 KB, and the About close-up loses nothing (its head samples level 1–2 of the old 2048² atlas anyway).
- *Ears.* Ear-weighted triangles whose normal is not within about 70° of forward (the rim and the back) had 48 % pink texels on the rim (5 % on the back); their texels go to the coat colour with 18 % of their own structure kept. The inside stays pink.
- *Verification.* Blender check sheet (`check-v6/`; Blender writes relative render paths under `C:\`, the renders were moved), `dev-mips-check.mjs` on the dev server (11 mip levels on the coat, eyes on their own texture, the blink swaps the eye map only, the sleeping cat in the bed from the hall camera and three close cameras, head close-ups open and closed), `dev-bed.mjs` (wake, out, back, settle into the curl), the turn, cat and companion regressions, the asset guard (7 tests, now checks the 1024² coat, the separate eye texture, the sheet and the closed texture), typecheck, build, live checksums.
- Known limits: the curl fills the ring only with the tail and the folded hind legs (a small hollow remains from straight above), p99 stretch 3.9, the coarse-level caps mean no whisker strokes on a cat under about 100 px, and the sparkle probe's residue is the eyes themselves.

### Responsive petting and coat detail (Phase 19, 2026-09-13)

Dennis judged Phase 18 live: clicking Blue was not fun or responsive enough; different parts should react like petting. The coat looked blotchy from the sides and front, with very dark patches beside lighter detailed areas. He suspected the last step. No new UI copy, model, atlas replacement, animation bake or paid assets.

- **Reproduced before editing.** Live browser inspection and dev probes in `.source-assets/blue/qa/petting-2026-09-13/` compared the shipped mip sheet, GPU-generated levels and the original source atlas at near and hall distances. Phase 18's majority-island downsampling discards the minority samples at every level; the brightness caps remove highlights as well. The new light-preservation test fails against v6b at the 256-pixel level (0.835 of the base's mean linear light). The original atlas also contains baked dark patches; not all tonal variation came from Phase 18. Touch overlays previously peaked only after 1.3 seconds, head/back responses on the ledge were identical, the wake path lost the touched region, and SkinnedMesh cached bounds could remain in the sleeping pose.
- **Coat.** `blue-v6-atlas.mjs` still starts from the chosen v6 export and its original atlas. It retains separate eye maps, fur-filled eye sockets, dark ear backs/rims and padded gutters. It lifts low-frequency dark fur locally within each UV island while retaining the original hue and fine hair contrast (maximum gain 2.5, exact black unchanged; eyes and ears excluded). Mips average in linear light, using a tent filter below 1024 to soften subpixel hair highlights. No majority selection, inner-ear replacement at distance, or luminance caps. This supersedes Phase 18's claim that its close-up lost nothing.
- **Touch.** Pointerdown responds before release. Hit regions use the actual hit triangle's skin weights; animated bone bounds keep picking valid after a pose change. Head: lean and slow blink; back: arch or seated stretch, smaller chest/head response on the ledge; tail: faster flick and ears back. Held strokes refresh an additive response rather than restarting it. Wake remembers the touched region; protected jumps retain their path. Mouse, pen, touch and keyboard use the existing target/label. The target has `touch-action: none`: the real vertical-touch probe failed before this change because the browser took over the gesture. The rest of the hall retains its scrolling behavior.
- **Verification.** `blue-petting.mjs`: real mouse press before release, all three regions on desktop and touch phone, 1.5-second mouse and vertical finger strokes, release, keyboard, sleeping-back hit and post-wake arch; no navigation or page errors. At 50 ms of stepped animation the response is already 0.593 of its target, rather than waiting for the old slow peak. All 96 frames of the final 12 reaction strips were viewed (standing/seated/sleeping/perched × head/back/tail), plus front/side near and hall views. The 36-frame hall strip was inspected in three complete pages. Its bright-pixel count is 38 versus 20 for the same v6b probe, concentrated at the moving eyes and inner ears; restored brightness makes this counter unsuitable as a pure fur-flicker score. Do not claim zero flicker or a numeric flicker improvement.
- **Release checks.** `blue-turn.mjs`, `blue-cat.mjs`, `blue-companion.mjs` pass, including desktop/phone/reduced motion, ledge petting and return. Their output was written outside the project under `%TEMP%/blue-petting-qa/regressions/` while browsers ran. Typecheck and all 35 review tests pass; the asset guard now checks light preservation across mip levels. Build/audit and post-upload results are recorded in the handovers.
- **Assets.** `BLUE_ASSET_BUILD = v6c`: GLB 1,206,368 bytes, mip sheet 76,288 bytes, closed eyes 2,036 bytes. All three filenames advance together and the v6b public files are removed. Geometry, rig, clips, correctives and painted eyes remain v6. Total 1,284,692 bytes.
- **Limits / next step.** The original photo atlas still has directional shading; this is a correction of its processing and dark patches, not a newly painted coat. Mobile vertical strokes must start on the existing 54-pixel touch target; horizontal strokes can also start on the actual mesh. The previous curl/bed/foot-slide limits remain. Dennis must judge the new coat and the feel on the live site before another visual change.

- **Publication.** App `199407c`, deployment `ceb80036` (https://ceb80036.dennis-portfolio-87g.pages.dev). Five routes return 200 on it and `www.dennisbf.design`; the three v6c files match `dist` by full SHA-256, CSP carries WASM/script hashes, the browser loads all three files and dispatches a real pet event with no console or request errors. The About screenshot exposed a separate existing production defect: after the soft transition the hall is absent beside the panel. Reproduced identically on Phase 18 `8973d2f7`; current dev shows the cabinet/cat. See the handovers and `about-probe.mjs`. Do not equate clean console with a clean About flow. The prebuild scanner's degraded sibling-repo snapshot was restored before rebuilding the committed app and passing the 52-page audit.

- A direct live `/about/` load on `www.dennisbf.design` shows the cabinet and Blue correctly with no page errors; its close-up was inspected. The separate production defect concerns the soft transition, not direct About rendering. Evidence: `live/www.dennisbf.design/about-direct.png`.

### Firefox coat upload and resting skull (Phase 20, 2026-09-13)

Dennis rejected the remaining sleep posture and sent a close-up of the face, chest and forelegs covered in hard black islands: "The caT SLEEPING is still in a weird position. research how cats actually sleep. ALSO the face is still super blotchy... with the blacks etc." This is his judgment of Phase 19, not an unjudged release any longer. Evidence for this round is under `.source-assets/blue/qa/sleep-coat-2026-09-13/`.

**Reproduce the actual browser and view.** The initial isolated, standing-cat Chromium probe did not reproduce his screenshot. The real direct `/about/` route in Firefox 153 on Windows did, with the current v6c files. Chromium 151 showed the same live files correctly. GPU-generated mips, removing sheen/specular, and an unlit material did not remove the black islands in Firefox. A flat unlit colour removed them; the original source atlas decoded separately also rendered correctly. `upload.mjs` then compared the current ImageBitmap, a canvas copy of that same bitmap, independent WebP/PNG decodes, raw pixel upload, and a canvas copy of every prepared mip. The canvas and pixel uploads rendered correctly without changing the decoded pixels (mean red 47.73279 in both the original bitmap and its canvas copy). This is a Firefox ImageBitmap-to-WebGL upload defect in the tested environment, not a reason to repaint the atlas or change the lighting. Phase 19's mip/light checks alone could not detect it.

**Fix.** `BlueCat.attachMipSheet` now copies the decoded base and every prepared level onto independent canvas surfaces and installs a fresh texture source. It keeps the same UVs, sRGB colour space, atlas, linear-light tent levels and material. No browser sniffing or per-frame work. `scripts/qa/blue-coat.mjs` freezes the real About scene and compares its rendered crop with a canvas-upload reference. It failed before the fix with **16,939 unexpectedly dark pixels**, then passed with **0 changed / 0 darkened pixels in both Chromium and Firefox**, including the final v6d asset. No console errors. Install the optional test browser with `npx playwright install firefox`; the test is run explicitly, alongside the three Blue regressions.

**Sleeping reference.** [Cats Protection's sleep guide](https://www.cats.org.uk/cats-blog/10-facts-about-cats-and-sleep) describes tightly curled, closed-eye deep sleep. The body-language guide also distinguishes relaxed side-lying from a tense crouch ([guide PDF](https://www.cats.org.uk/media/bfljilgc/vet_1175-eg_understanding-cat-behaviour_a6_24pp_w84009_digi.pdf)). Two actual black-cat photographs were opened and visually inspected: [cat on a pillow](https://commons.wikimedia.org/wiki/File:Black_Cat_sleeping_on_a_pillow.jpg) and [Boo curled up](https://oliveoctopus.ink/boo.htm). Observations from those photographs: the side of the torso carries the weight, the cheek/chin is supported, the legs fold into the body curve, and the head is not held up for the camera. These were references only; no external photo or model was added to the site.

**Pose.** The old shortest-rotation torso frame twisted shoulder and pelvis inconsistently through the curl. The head half-followed the neck, then used a compensating rotation to face the room; the paws and knee/elbow bend hints remained in their standing orientation. The new curl uses a consistent lateral frame, rolls onto its side, and gives the skull its own horizontal heading and cheek-down roll. The paws and bend hints follow the resting torso. The legs tuck into a compact curve and the tail follows its inner edge. Settle and wake interpolate through the same pose. The chosen v6 mesh, skeleton, painted eyes and other movement clips stay. Five pose-lab iterations preceded the bake. All 13 final lab frames, all 60 baked check frames and all 43 dev-runtime frames were inspected, including the actual hall view, close/side/top sleep, settle and wake. No claim that the rig now has perfect anatomy: the existing folded-skin limitations remain (sleep p99 stretch 3.715; a small underside penetration of about 11 mm in the lab; a small inner gap from above).

**Validation and assets.** `blue-turn.mjs`, `blue-cat.mjs`, `blue-companion.mjs`, `blue-petting.mjs` and the new Chromium/Firefox coat test pass. Desktop, phone and reduced motion, ledge petting, wake-region memory and return paths are covered. The regressions wrote outside the repository under `%TEMP%/blue-sleep-coat-qa/`; all custom dev captures buffered files until their browser closed. Typecheck, 35 review tests, build and the 52-page audit pass. The sandboxed prebuild scanner again downgraded sibling repositories; its generated `werkstatt.json` was restored from the untouched snapshot before rebuilding the application commit.

The three files advance together to `v6d`: GLB **1,200,120 B**, mip sheet **76,288 B**, closed eyes **2,036 B** (total **1,278,444 B**). Old v6c public filenames are removed. The embedded coat and eye images are byte-identical to v6c (coat SHA-256 `f4e474d38eadff3b516889c874678e12c40177ccf1ad69a783713d4297a5309b`, eyes `ec360353866d1a998905c37cbc85e6415b4dce60c5394c69e1237ba704cb37b5`). No UI copy, new mesh, atlas replacement or paid asset.

**Publication.** Application `d85ce67`, deployment [`107e5340`](https://107e5340.dennis-portfolio-87g.pages.dev), also verified on `www.dennisbf.design`. Both hosts: five routes at 200; all three v6d files match dist by full SHA-256; CSP includes `'wasm-unsafe-eval'` and script hashes. Firefox and Chromium headless runs loaded the assets, dispatched a real pet event, and captured home/wake/direct About with no page or console errors and no failed requests. Firefox warnings are retained in the reports: the lost contexts were the detached 300×150 feature probes deliberately released by `HallGuard.astro`; no displayed canvas lost its context. Missing parallel-shader extension, an upload deprecation and a font-preload warning are also recorded. The earlier production About soft-transition defect remains separate and unfixed. Evidence: `.source-assets/blue/qa/sleep-coat-2026-09-13/live/`. Dennis must judge the new live sleep and coat.

### Petting cadence and continuous head motion (Phase 21, 2026-09-13)

Dennis: "You can make the cat move rly fast and janky. should be more polished so thats not possible. in general more polish", then "Wenn man den kopf streichelt dann flippt er hin und her ohne animation auch". This round changes runtime interaction and transitions only; all three v6d assets stay byte-identical.

**Reproduction.** A real-pointer dev probe alternated head/back at five 60 Hz steps per click, spammed the tail, touched during a turn and alternated sides of the head. 24 alternating touches caused 23 full-body animation switches; the head-side test peaked at 23.38 degrees in one frame. The pose strip visibly flipped left/right. A production direct-About strip was also inspected (the first crop missed the cat and was discarded; the corrected strip follows the pet button). Evidence: `.source-assets/blue/qa/polish-2026-09-13/`; corrected baseline live strip under `%TEMP%/blue-polish-release-qa/.source-assets/blue/qa/polish-2026-09-13/live-before/`.

**Causes and fix.** The touch envelope was smooth but the side sign and head/back/tail angle targets were discrete, so changing regions while fully petted jumped the bones. Pet angles now ease at damping 8 with a maximum 1.2 rad/s change; chest/ear overlays follow the same rule. Every touch still feeds the response immediately. Full-body head/back reactions previously interrupted each other and played at 1.5x; tail flicks reset on each click at 1.6x. They now play at 1x, the head phrase has a 1.4 s minimum before a back stretch, an arch completes before a head reaction, and the tail completes plus 0.35 s rest before another flick. Walking, turning, sitting down, perching, standing up, unperching and mapped jumps are acknowledged through the soft response without cancelling their paths. No input backlog.

The first smoothing change exposed a separate 10–20 degree transition jerk: inertialization stored the pose after gaze/petting, then the next frame carried that overlay over and applied it again. Its history now stores the animation pose before attention. This fixes the transition itself rather than hiding it with a longer cooldown.

**Measured outcome.** Same original probe: alternating touches 23 → 1 full-body switch; maximum head-side delta 23.38 → 2.69 degrees/frame. The permanent `blue-polish.mjs` uses a coherent goal for its protected-turn case and measures 2.72 degrees/frame in the head-side test; alternating reactions 2.45 degrees/frame. It checks hit coverage, bounded motion, no tail reset, 1x reaction playback and uninterrupted turn/walk. All 96 final regression frames were inspected. All three required Blue regressions, `blue-petting.mjs`, `blue-polish.mjs`, typecheck, 35 review tests, build and the 52-page audit pass. Required dev scripts ran from `%TEMP%/blue-polish-release-qa/` to keep writes outside the project. Prebuild's degraded Werkstatt snapshot was restored, then the committed app was rebuilt and audited. Home reachable JS 1338.4 KB, CSS 174.8 KB.

**Publication.** Application `da940da`, deployment [`e354e82e`](https://e354e82e.dennis-portfolio-87g.pages.dev), also verified on `www.dennisbf.design`. Both hosts and both Firefox/Chromium: five routes return 200, three v6d assets and the actual Stage3D runtime match dist by SHA-256, CSP includes WASM and script hashes, headless loads and rapid real head clicks have no page/console errors or failed requests. Startup warnings remain recorded separately; displayed contexts stay intact. All 48 final live petting frames were inspected. The existing About soft-transition issue remains separate. Dennis must judge the new interaction live. No UI copy, model, atlas, clip or paid-asset change.

Live-QA correction: the first rapid-head check used an offset from the body button and missed the head, triggering the normal backdrop/back action and eventually hiding the button. Its captured missing-target state is a test-coordinate error, not a new disappearing-cat defect. The final probe uses the head coordinates verified in the 1440×900 live screenshot (x 220/250, y 165); all four host/browser runs dispatch 24 pet events and keep Blue on the ledge.

### Meshy toys and supported sleep transitions (Phase 22, 2026-09-14)

Dennis requested clickable toys by the bed, generated reference images → Meshy → Blender cleanup, lightweight physics and a funny fidget interaction. He also judged the curling transition creepy and unnatural. He approved exactly these accessible names: “Ball anstupsen” / “Nudge ball”, “Stoffmaus anstupsen” / “Nudge toy mouse”. No other UI copy was added.

**Reproduction and sleep research.** The live home view and a 25-frame dev settle probe were inspected before editing. Interpolating torso joint positions and subsequently repairing their distances made the intermediate chest/neck collapse and rotate awkwardly. The solver now interpolates segment directions with slerp while preserving segment lengths. Settle takes 4.6 s: supported lowering and leg tuck first, then curl from 1.75–4.4 s. Wake takes 2.8 s: uncurl while low, then push up through the forelegs and hindquarters. Ground correctives follow those times. A second defect appeared at the sleep-loop boundary: a reused action retained zero effective weight after a previous fade. `enter()` explicitly restores weight before fading in; the cat no longer abruptly stands when returning to that sleep action.

The research references were [Cats Protection: cats and sleep](https://www.cats.org.uk/help-and-advice/cat-behaviour/cats-and-sleep) and its [Understanding cat behaviour guide](https://www.cats.org.uk/media/bfljilgc/vet_1175-eg_understanding-cat-behaviour_a6_24pp_w84009_digi.pdf). Relaxed side resting and curled resting inform the supported sequence; the exact animation is an authored interpretation, not motion capture. The v6 mesh still has folded-skin limitations: final sleep p99 stretch approximately 3.734 and around 1 cm underside penetration in the lab. The small hollow from directly above remains. Dennis must judge the revised transition on the live site.

**Toys.** The built-in image generator made a lavender/cream felt ball and gray felt mouse with lavender ears and short cream tail. Selected images, normalized prompt briefs, Meshy request records, originals and cleaned GLBs are retained in `.source-assets/blue/toys-2026-09-14/` (see README.md). [Meshy image-to-3D](https://docs.meshy.ai/en/api/image-to-3d): Meshy 7 standard, triangle remesh targets 1,500 / 2,500, texture enabled, no PBR maps, GLB only. Two tasks at 30 credits each. The credential was used only as MESHY_API_KEY, never written to the project. Blender bakes transforms, normalizes dimensions/origins and smooths normals; packing uses 512px WebP and Meshopt. `blue_toys_clean.py` and `blue-toys-pack.mjs` retain that pipeline. The packer isolates sharp in a subprocess because its libvips build conflicts with the glTF toolchain's native image library in one process.

| Asset | Triangles | Bytes |
| --- | ---: | ---: |
| Ball v1 | 1,525 | 61,572 |
| Mouse v1 | 2,623 | 104,956 |
| Blue v6e GLB, now 25 clips | unchanged mesh | 1,242,228 |
| Blue v6e mip sheet | — | 76,288 |
| Blue v6e closed eyes | — | 2,036 |

The toys load after Blue without blocking the hall reveal. `blueToys.ts` runs fixed 1/120 s physics, gravity, damped rebound, rolling, toy contacts and a bed collider. Time steps and input speed are capped; inputs have a short cooldown. The play area stays by the bed. On narrow screens both toys start on its visible right side. Buttons follow their projected locations, support mouse/touch/keyboard and carry the approved DE/EN names. Reduced motion allows only a small nudge with no bounce/chase. Paused/hidden scenes accumulate no physics time.

Blue wakes, completes the mapped bed exit, turns and approaches the selected toy, then uses a new 1.15 s authored forepaw swat. The impulse occurs once at paw contact. Existing travel, turns and paw strokes finish before accepting another approach; rapid input cannot restart them continuously. Play lasts up to 18 s after the latest nudge, with a rest between strokes and smoothed attention toward the toy. Navigation returns control to the existing companion plan.

**Validation.** All 25 before frames, 49 after frames, six targeted lab views, 60 baked standard views, 45 runtime play frames and 96 polish frames inspected. Required blue-turn, blue-cat and blue-companion regressions pass, as do petting, polish and Chromium/Firefox coat comparisons (zero changed/darkened pixels against the canvas reference). New blue-toys regression passes both engines × desktop/mobile/reduced motion: real ball input, keyboard mouse input, both toys swatted, finite bounded physics, rapid input, pause and reused sleep-action weight. The first mobile probe exposed an offscreen ball; final placement fixes it. Typecheck, 35 review tests, build and 53-page audit pass. The sandboxed prebuild scan degraded Werkstatt data again; the original snapshot was restored before rebuilding application commit `753581a` and auditing its exact build. Home reachable JS 1,344.5 KB; CSS 175.2 KB.

The three Blue filenames advance together from v6d to v6e; old public filenames are removed. The mip sheet and closed-eye image are byte-identical to v6d. The selected original v6 model and original atlas remain; this round changes animation, not coat design. The previous production About soft-transition issue remains separate.

**Publication.** Application `753581a`, deployment [e5f2a08d](https://e5f2a08d.dennis-portfolio-87g.pages.dev), also verified on [www.dennisbf.design](https://www.dennisbf.design). Both hosts in Chromium and Firefox: five routes at 200, CSP includes WASM, all five model files match dist SHA-256, real desktop/mobile ball input and keyboard mouse input respond without navigation, direct About renders and hides toys. No page/console errors or failed requests. Known Firefox feature-probe context-release, upload and missing-extension warnings are recorded. All 36 live captures inspected; timed captures can include the ongoing ledge arrival. Sources, prompts and evidence remain in `.source-assets/blue/toys-2026-09-14/`; `live.mjs` is the verification entry point. Dennis must judge the new toys and transition live.

The embedded coat and eye image hashes also remain identical to v6d: coat `f4e474d38eadff3b516889c874678e12c40177ccf1ad69a783713d4297a5309b`, eyes `ec360353866d1a998905c37cbc85e6415b4dce60c5394c69e1237ba704cb37b5`.
